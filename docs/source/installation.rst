Installation
=====

.. _installation:

Installation
------------

To use AML, first install it from git repository:

.. code-block:: console

   pip install -e .

For tests first install Tox:

.. code-block:: console

    pip install tox

and then execute:

.. code-block:: console

    tox -e test_pipeline
